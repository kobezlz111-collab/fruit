# 轻账本 · 个人财务记账 Web 版
## 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.0 |
| 产品版本 | V1.0（首个可上线版本） |
| 文档作者 | 产品经理 |
| 创建日期 | 2026-08-18 |
| 文档状态 | 待评审 |
| 目标平台 | Web 网页端（手机浏览器优先，桌面自适应） |

---

## 一、产品概述

### 1.1 产品背景

大部分人对自己的钱花在哪里缺乏清晰的认知，「钱不知道花哪了」是普遍痛点。市面上主流记账 App 功能繁杂、广告多、学习成本高，很多用户记几笔就放弃。同时微信/支付宝的账单分散在多个渠道，无法统一查看整体消费情况。

### 1.2 产品定位

一款**打开即用、10 秒记一笔、快速出报表**的轻量个人记账工具。V1 以 Web 网页端形态上线，用户在手机浏览器中访问即可使用，无需下载安装；后续迭代演进为移动 App 与小程序。

**一句话描述**：手机上打开就能记，月底一看就知道钱花哪儿了。

### 1.3 产品目标

| 目标维度 | 具体目标 |
| --- | --- |
| 记账效率 | 完成一笔记录的**平均耗时 ≤ 15 秒** |
| 上手成本 | 新用户 **3 分钟**内完成第一笔记录并看到报表 |
| 留存 | 记账后 **7 日留存率 ≥ 30%** |
| 数据安全感 | 提供一键导出，用户随时可以带走自己的数据 |

### 1.4 核心价值

1. **极速记账**：默认支出、常用分类置顶、大数字键盘，减少每一步操作。
2. **所见即所得**：记完立即能看到今日/本月汇总与分类占比，正反馈即时。
3. **数据归集**：统一记录现金、微信、支付宝、银行卡等所有渠道的收支。
4. **数据自有**：V1 无账号体系，数据本地存储，支持一键导出，不绑架用户。

---

## 二、目标用户与使用场景

### 2.1 用户画像

| 用户类型 | 特征 | 核心诉求 | V1 支持程度 |
| --- | --- | --- | --- |
| **核心用户**：职场年轻人 / 上班族（18~35 岁） | 手机重度用户，消费场景多（外卖、交通、网购），想省钱但缺乏复盘工具 | 快速记账、月度消费分析 | ✅ 完整支持 |
| **次要用户**：家庭 / 情侣 | 关注家庭整体开支，偶尔两人同时记账 | 统一账本、按场景看开销 | ✅ 单账本支持；多人共享留待 V2 |
| **延伸用户**：小微创业者 / 个体户 | 有经营收支记录需求，但记账工具偏个人、无需进销存 | 经营支出/收入分类记录 | ⚠️ 通过自定义分类轻量支持；独立「经营账本」留待 V2 |

### 2.2 用户场景故事

**场景一 · 随手记**
> 小张是程序员，下班后去便利店买晚饭花了 23.5 元。他掏出手机，打开浏览器里已收藏的记账页面，默认就是「支出」，金额键盘已弹出，他输入 23.5，选了置顶的「餐饮」分类，点保存。整个过程不到 10 秒。

**场景二 · 月底复盘**
> 月底小张打开「统计」页：本月支出 4860 元，其中「外卖」分类占 28%。他点开该分类下钻，看到 15 笔外卖记录，意识到开销大头，决定下月少点外卖。

**场景三 · 跨渠道归集**
> 小张平时微信、支付宝、现金混着花。他把三张「账户」都建好，每笔记录选择对应账户。月底报表把所有渠道的消费合并统计，支出总览一目了然。

**场景四 · 个体户轻量记账**
> 阿强开了家小店，他自定义了「进货」「店租」「水电」等分类，把每日进货当「支出」记、收款当「收入」记，月底看月度报表了解经营盈亏。

---

## 三、竞品简析

| 产品 | 优势 | 不足 | 可借鉴 |
| --- | --- | --- | --- |
| 随手记 / 鲨鱼记账 | 功能全、生态成熟 | 功能重、广告多、有学习成本 | 分类体系、报表维度 |
| 微信 / 支付宝自带账单 | 无记录成本、渠道内自动归集 | 只能看单一渠道，无法统一、不可编辑归类 | 「从微信/支付宝导入账单」可作为 V2 差异化功能 |
| 极简记账（One Day One Budget 等） | 极快、无干扰 | 功能弱、无报表深度 | **「10 秒记一笔」的交互范式是本产品核心** |

**差异化结论**：以「极致快捷记账 + 清晰月度报表」为核心，克制功能，无广告，数据可导出可带走。

---

## 四、范围界定

### 4.1 V1 范围（做）

| 模块 | 内容 |
| --- | --- |
| 记账 | 支出 / 收入 / 转账 三类记录，金额、分类、账户、日期、备注 |
| 账单管理 | 流水列表、筛选、搜索、编辑、删除 |
| 账户管理 | 多账户（现金、微信、支付宝、银行卡、信用卡）、余额自动计算 |
| 分类管理 | 预设分类体系 + 自定义分类（增删改、排序、图标） |
| 统计报表 | 月度收支概览、分类占比、趋势图、分类排行 |
| 数据管理 | 导出 CSV / Excel、本地备份（JSON）与恢复 |

### 4.2 V1 不做（明确排除）

| 不做项 | 原因 |
| --- | --- |
| 账号体系 / 云端同步 / 多设备 | 需要后端，超出 V1 范围；V1 数据本地存储 |
| 多人共享账本 | 依赖账号体系，V2 再规划 |
| 预算管理与超支提醒 | 明确不在本次范围（用户确认） |
| 资产 / 负债 / 借贷管理 | 明确不在本次范围 |
| 图片小票、语音记账、AI 分类 | 后续版本特性 |
| 微信 / 支付宝账单导入 | 需要对接第三方，V2 差异化能力 |

### 4.3 版本规划

| 版本 | 范围 | 形态 |
| --- | --- | --- |
| **V1（本期）** | 记账 + 统计报表 | Web 网页端（手机浏览器优先） |
| V2 | 账号体系、多设备同步、预算管理、账单导入、多人共享 | Web + 小程序 |
| V3 | 资产管理、报表增强、AI 智能分析 | Web + App + 小程序 |

---

## 五、功能需求详述

> 优先级说明：P0 = 必须（本期核心，缺失则不可上线）；P1 = 重要（本期应有）；P2 = 可选（有余力则做）。

### 5.1 F1 记账（P0，核心）

**F1.1 新增账单**
- 账单类型三选一：**支出 / 收入 / 转账**（默认「支出」）。
- 必填字段：**金额**（支持两位小数，正整数部分最大 9,999,999.99）。
- 可填字段：分类、账户、日期时间、备注（≤ 100 字）。
- 支出默认分类为「未分类」（已设置常用分类时，自动带出上次使用的分类）。
- 保存成功提示轻量反馈（Toast），并停留在当前页以支持**连续记账**。

**F1.2 快速记账交互**
- 进入首页即进入记账态：金额输入框聚焦、**大数字键盘**弹出。
- 输入金额后自动定位到分类选择（常用分类置顶，优先显示）。
- 目标：单笔记录操作步数 ≤ 3 步（输入金额 → 选分类 → 保存）。

**F1.3 转账**
- 从账户 A 转账至账户 B：选择「转出账户」与「转入账户」，填写金额与日期。
- 转账**不计入**收入/支出，仅影响两账户余额。
- 两个账户不可相同。

**F1.4 编辑与删除**
- 账单详情页可编辑全部字段；修改后重新统计。
- 删除需**二次确认**，删除后不可恢复（提示用户先导出备份）。
- 删除单笔支持；批量删除不做（V1 低概率需求）。

**F1.5 时间处理**
- 默认取当前时间，可修改日期（支持快速选「今天/昨天」）。
- 跨天记账正常处理，报表按账单实际日期归组。

### 5.2 F2 账单管理（P0）

- **流水列表**：按日期倒序分组展示，显示每笔的账户、分类、备注、金额（颜色遵循「七、视觉设计规范」功能色：支出薄荷绿 `#6FC7A1` / 收入蜜桃橘 `#FF8A5C` / 转账灰蓝 `#8E9BB3`）。
- **当日小计**：每组日期显示当日支出与收入合计。
- **筛选**：按 类型（支出/收入/转账）、分类、账户、日期范围 组合筛选。
- **搜索**：按备注内容与金额关键词搜索。
- 列表支持下拉刷新与上拉加载更多（分页加载，单页 20 条）。

### 5.3 F3 账户管理（P0）

- 默认账户（系统预置，可删除/改名）：**现金、微信、支付宝、银行卡、信用卡**。
- 新增 / 编辑 / 删除账户：字段为 名称、类型（现金/银行卡/信用卡/支付宝/微信/其他）、初始余额、图标（默认 emoji）、排序。
- **余额自动计算**：账户余额 = 初始余额 + 收入合计 − 支出合计（转账自动结算进出）。
- 信用卡作为「负债账户」类型展示，余额可为负数，不计入资产统计（V1 仅展示，不做负债模块）。
- 删除账户前提示：该账户下的历史账单需先转移至其他账户或确认数据影响。

### 5.4 F4 分类管理（P0）

**F4.1 预设分类体系（两级，一级分类可展开）**

支出分类：

| 一级分类 | 二级分类示例 |
| --- | --- |
| 餐饮 | 三餐、外卖、饮品、零食 |
| 交通 | 公交地铁、打车、加油、停车 |
| 购物 | 日用百货、服饰、数码、美妆 |
| 居住 | 房租、水电燃气、物业、家居 |
| 娱乐 | 电影、游戏、旅游、运动 |
| 医疗 | 药品、门诊、体检 |
| 教育 | 课程、书籍、培训 |
| 人情 | 送礼、红包、聚会 |
| 通讯 | 话费、网费 |
| 其他 | — |

收入分类：**工资、奖金、理财、兼职、礼金、报销、其他**。

**F4.2 自定义分类**
- 支持新增一级/二级分类，可改名、更换图标、调整排序。
- 自定义分类数量上限 50 个，防止体系失控。
- 系统预置分类不可删除，可隐藏；自定义分类可删除（删除后其下账单归入「未分类」）。

**F4.3 常用分类**
- 记账频次高的分类自动置顶，减少选择成本。

### 5.5 F5 统计报表（P0，核心差异化）

**F5.1 概览卡片**
- 本月收入 / 支出 / 结余，与上月环比（↑↓ 百分比）。
- 日均支出、平均每天记 N 笔（可后置 P1）。

**F5.2 分类占比**
- 支出分类占比**环形图**（Top 8 + 其他），点击分类下钻到该分类账单列表。
- 收入分类占比（P1）。

**F5.3 收支趋势**
- 折线 / 柱状趋势图，支持**按日 / 按周 / 按月**切换。
- 支出与收入双序列对比。

**F5.4 分类排行**
- 支出分类金额 Top 10 排行列表（金额 + 占比 + 笔数）。

**F5.5 时间范围**
- 支持 本月 / 上月 / 近 3 个月 / 近 12 个月 / 自定义日期区间。
- 报表数据实时基于当前账单计算，无缓存过期问题。

**F5.6 图表规范**
- 图表需清晰展示数据标签与 Tooltip；环形图支持图例。
- 遵循移动端触控交互（可点选、可下钻）。

### 5.6 F6 数据管理（P0）

- **导出**：一键导出为 **CSV / Excel** 文件，包含全部账单明细（类型、金额、分类、账户、日期、备注）。
- **备份与恢复**：导出完整数据包（JSON），支持导入恢复。
- **数据说明**：设置页明确告知「数据仅存储在本设备浏览器中，清除浏览器数据会丢失，请定期导出备份」。
- 首次使用后 7 天内、以及记账达到 100 笔时，各触发一次导出备份引导。

### 5.7 F7 设置（P1）

| 设置项 | 说明 |
| --- | --- |
| 主题 | 浅色（樱粉主题 · 默认）/ 深色 / 跟随系统 |
| 货币符号 | 默认 ¥，支持切换（$、€ 等） |
| 一周起始日 | 周一 / 周日（影响按周统计） |
| 默认账户 | 记账时默认带出的账户 |
| 语言 | V1 仅简体中文 |
| 关于 / 隐私说明 | 说明数据本地存储、无账号无采集 |

---

## 六、信息架构与页面结构

### 6.1 导航结构

移动端采用底部 Tab 导航（4 个）：**记账（首页） · 流水 · 统计 · 我的**

```
轻账本（Web · 手机浏览器）
├── 记账（首页，默认）
│     └── 顶部：本月收入/支出/结余概览
│     └── 中部：记账表单（金额键盘 + 分类 + 账户）
│     └── 最近记录（最近 5 笔，可跳转流水）
├── 流水
│     └── 筛选栏 + 日期分组账单列表 + 搜索入口
│     └── 账单详情页（编辑/删除）
├── 统计
│     └── 时间范围切换 + 概览卡片 + 分类占比 + 收支趋势 + 分类排行
└── 我的
      └── 账户管理
      └── 分类管理
      └── 数据管理（导出/备份/恢复）
      └── 设置
      └── 关于
```

### 6.2 页面流转

- 底部 Tab 全站可见、可即时切换。
- 流水/统计页点击分类或图表可下钻至对应的账单筛选列表。
- 所有弹层（分类选择、日期选择、账户选择）使用移动端底部弹出式组件，避免新开页面。

---

## 七、视觉设计规范（配色）

### 7.1 设计关键词

**粉嫩、可爱、治愈、轻盈**。整体视觉以「奶油粉 + 玫瑰粉」为基调，营造温柔、无压力的使用氛围。**粉色作为强调色而非大面积铺满**，底色保持奶油白，保证信息清晰与文字可读性。

### 7.2 色彩体系

**品牌主色（粉系）**

| 用途 | 色名 | 色值 | 使用说明 |
| --- | --- | --- | --- |
| 品牌主色 | 玫瑰粉 | `#FF6F91` | 主按钮、选中态、底部 Tab 激活、链接、关键高亮 |
| 品牌深色 | 蔷薇红 | `#F2567E` | 按钮按压态、渐变深端 |
| 品牌浅色 | 樱花粉 | `#FFD3E0` | 标签底色、浅色信息块、选中态背景 |

**辅助色（可爱点缀）**

| 色名 | 色值 | 用途 |
| --- | --- | --- |
| 蜜桃 | `#FFB38A` | 次要按钮、标签、点缀 |
| 薰衣草紫 | `#B9A7E3` | 分类图标点缀 |
| 天空蓝 | `#8FC7E8` | 分类图标点缀 |
| 柠檬黄 | `#F7D774` | 高亮、徽章 |

**功能色（财务语义色，全站统一）**

| 语义 | 色名 | 色值 | 用途 |
| --- | --- | --- | --- |
| 收入 | 蜜桃橘 | `#FF8A5C` | 收入金额、收入分类、收入图表 |
| 支出 | 薄荷绿 | `#6FC7A1` | 支出金额、支出分类、支出图表 |
| 转账 | 灰蓝 | `#8E9BB3` | 转账金额（中性色，不参与收支语义） |
| 成功 | 薄荷绿 | `#6FC7A1` | 成功提示 |
| 警告 | 蜜杏橙 | `#F7B267` | 警告提示 |
| 错误 | 珊瑚红 | `#F07167` | 错误提示 |

**文字与背景**

| 用途 | 色值 |
| --- | --- |
| 主文字 | `#4E3B44`（暖深灰） |
| 次级文字 | `#8A7580` |
| 弱化 / 占位 | `#C4B4BC` |
| 页面背景 | `#FFF8FA`（奶油粉白） |
| 卡片背景 | `#FFFFFF` |
| 分割线 | `#F5E6EB` |

**报表图表色板（柔和粉彩）**

`#FF9AA2` 粉 · `#FFB7B2` 浅珊瑚 · `#FFDAC1` 蜜桃 · `#E2F0CB` 浅黄绿 · `#B5EAD7` 薄荷 · `#C7CEEA` 雾蓝 · `#E6B9E3` 浅紫 · `#F6D365` 柠檬黄

### 7.3 使用规范

1. **粉色克制使用**：主色用于关键操作与高亮；大面积底色使用奶油粉白，避免高饱和粉大面积铺满造成廉价感。
2. **可读性**：正文与背景对比度满足 WCAG AA（≥ 4.5:1）；次级文字仅用于辅助信息，不使用过浅文字承载关键金额。
3. **功能色一致性**：收入 / 支出 / 转账颜色全站统一，列表、报表、分类图标使用同一语义色，不因页面不同而变换。
4. **深色模式（P1）**：以深暖灰紫（如 `#2A2226`）为底，品牌粉保留低饱和版本；V1 至少保证浅色主题完整落地。
5. **可爱元素**：分类图标使用圆润 emoji；卡片大圆角（≥ 12px）、柔和阴影、按钮圆润；插画风空状态（如「今天还没有记录哦～」）。

---

## 八、数据模型（草案）

```
Transaction 账单
├── id: string            // 唯一ID（UUID）
├── type: 'expense' | 'income' | 'transfer'
├── amount: number        // 金额（≥0，两位小数）
├── categoryId: string    // 分类ID（转账可为空）
├── accountId: string     // 账户ID
├── toAccountId: string   // 转账目标账户ID（仅转账时使用）
├── date: string          // 账单日期 YYYY-MM-DD
├── time: string          // 账单时间 HH:mm
├── note: string          // 备注（≤100字）
├── tags: string[]        // 预留标签（V1 可不开放）
├── createdAt / updatedAt // 创建/更新时间
└── deletedAt             // 软删除标记

Category 分类
├── id: string
├── name: string
├── type: 'expense' | 'income'
├── icon: string          // emoji
├── parentId: string|null // 一级分类为 null
├── isSystem: boolean     // 是否系统预置
├── isHidden: boolean     // 隐藏的系统分类
├── sort: number
└── useCount: number      // 使用频次（用于常用分类置顶）

Account 账户
├── id: string
├── name: string
├── type: 'cash' | 'bank' | 'credit' | 'alipay' | 'wechat' | 'other'
├── icon: string
├── initialBalance: number
├── sort: number
└── isHidden: boolean

Settings 设置
└── theme / currency / weekStart / defaultAccountId ...
```

数据存储：**IndexedDB**（结构化、支持大数据量），导出时序列化为 CSV / JSON 文件。

---

## 九、非功能需求

### 8.1 兼容性与适配

| 项 | 要求 |
| --- | --- |
| 目标浏览器 | Chrome、Safari、Edge、微信内置浏览器（iOS / Android） |
| 屏幕适配 | 375px ~ 414px 手机宽度**优先**；桌面浏览器 ≥ 1024px 自适应排版 |
| 交互基准 | 触控优先，按钮点击区 ≥ 44×44px |

### 8.2 性能

| 指标 | 目标 |
| --- | --- |
| 首屏加载 | 4G 网络下 ≤ 3s |
| 记账保存响应 | ≤ 200ms（纯本地，无网络请求） |
| 报表渲染 | 1 万笔账单下统计页 ≤ 1s |
| 数据规模 | 单用户 10 万笔账单流畅操作 |

### 8.3 可靠性

- **PWA 离线可用**：支持添加到主屏幕、离线记账，联网后数据不丢失。
- 数据仅存本地，无后端、无账号、无第三方统计采集（隐私友好是卖点）。
- 导出备份引导机制兜底本地数据丢失风险。

### 8.4 安全

- 本地数据不随网络传输；如后续引入同步，金额等数据需加密传输与存储。
- 删除操作一律二次确认。

---

## 十、埋点与成功指标

| 指标 | 定义 | 目标 |
| --- | --- | --- |
| 记账完成数 / 活跃用户 | 人均每日记账笔数 | ≥ 1.5 笔 |
| 单笔记账耗时 | 从进入记账到保存的时间 | ≤ 15 秒 |
| 次日 / 7 日留存率 | 使用过并再次回访 | 次日 ≥ 40%，7 日 ≥ 30% |
| 报表查看率 | 记过账的用户中查看过统计的比例 | ≥ 60% |
| 导出率 | 使用过导出/备份功能的用户比例 | ≥ 10%（验证数据安全感） |

---

## 十一、风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 本地数据丢失（清除缓存/换设备） | 用户全部记账数据丢失，信任崩塌 | 强导出/备份引导；PWA 存储尽量持久化；V2 上线账号同步 |
| 换设备数据无法迁移 | 用户迁移成本高，流失 | V1 提供 JSON 备份恢复；V2 云端同步 |
| 记账动力不足、弃用率高 | 留存差 | 极速记账降低摩擦；即时报表正反馈；后续版本加预算提醒等钩子 |
| 竞品同质化 | 缺乏竞争力 | 聚焦「极速记账 + 数据自有」差异化，不堆功能 |
| 个体户/企业需求误入个人产品范围 | 功能膨胀、定位漂移 | V1 仅通过自定义分类轻量覆盖；独立经营账本规划至 V2 |

---

## 十二、验收标准（V1 上线 checklist）

1. **记账**：支出、收入、转账均可正常创建/编辑/删除；金额与分类必填校验生效；记一笔 ≤ 15 秒。
2. **账户**：预置 5 个默认账户；余额随账单自动更新且计算正确（含转账）。
3. **分类**：预置分类完整可用；自定义分类可增删改排序；删除分类后账单归入「未分类」。
4. **报表**：概览、分类占比、趋势图、分类排行在 iOS Safari、Android Chrome、微信内置浏览器三端渲染正常；数据与账单一致；点击可下钻。
5. **数据导出**：CSV / Excel 导出内容完整、字段正确、可用 Excel / Numbers 正常打开；JSON 备份可恢复且恢复后数据无丢失。
6. **兼容性**：375px 宽度下无横向滚动、无遮挡，核心操作可触达。
7. **离线**：断网环境下记账与查看仍可用（PWA）。
8. **性能**：首屏加载 ≤ 3s；1 万笔账单下报表页 ≤ 1s。
9. **隐私**：页面不发起任何非必要网络请求；无账号体系。
10. **删除安全**：所有删除均有二次确认。
11. **视觉与配色**：全站配色遵循「七、视觉设计规范」；收入 / 支出 / 转账功能色全局一致；品牌粉用于关键操作；正文与背景对比度满足可读性要求。

---

## 十三、附录

- 本文档为 V1.0，需求以评审结论为准；评审确认后进入开发排期。
- 需求变更统一记录于后续版本号（V1.1、V1.2…）。
